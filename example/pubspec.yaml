name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '^3.4.0'

dependencies:
  flutter:
    sdk: flutter
  vmate:
    path: ..
  signals_flutter: ^6.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
